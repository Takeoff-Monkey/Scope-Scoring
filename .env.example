# AWS Configuration
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1

# ECR Repository Name
ECR_REPOSITORY_NAME=erw-job-scorer

# AWS Authentication (choose one method):

# Option 1: Use a named AWS CLI profile (run 'aws configure --profile myprofile' first)
AWS_PROFILE=

# Option 2: Use access keys directly (leave AWS_PROFILE empty)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Option 3: If running on EC2/ECS with an IAM role, leave all auth fields empty
